apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: we-usage-tracker-data
  namespace: default
spec:
  # Keep the worker state on a single Longhorn volume.
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
