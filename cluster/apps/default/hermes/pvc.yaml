apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hermes-data
  namespace: default
  labels:
    app.kubernetes.io/name: hermes
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 8Gi
