apiVersion: v1
kind: Service
metadata:
  name: openchamber
  namespace: default
  labels:
    app.kubernetes.io/name: openchamber
    app.kubernetes.io/component: ui
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: opencode
    app.kubernetes.io/component: backend
  ports:
    - name: http
      port: 3000
      targetPort: openchamber
