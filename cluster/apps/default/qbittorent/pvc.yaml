apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qb-config-pvc2
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 200Mi
