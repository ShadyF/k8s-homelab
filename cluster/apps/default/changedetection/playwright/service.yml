apiVersion: v1
kind: Service
metadata:
  name: playwright-chrome
  namespace: default
  labels:
    app: playwright-chrome
spec:
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: playwright
  selector:
    app: playwright-chrome