apiVersion: v1
kind: Service
metadata:
  name: sockpuppet-chrome-svc
  namespace: default
  labels:
    app: sockpuppet-chrome
spec:
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: sockpuppet-chrome