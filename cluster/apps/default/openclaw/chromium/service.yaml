apiVersion: v1
kind: Service
metadata:
  name: chromium
  namespace: default
spec:
  ports:
    - name: debug
      port: 9222
      targetPort: 9223
      protocol: TCP
  selector:
    app: chromium
