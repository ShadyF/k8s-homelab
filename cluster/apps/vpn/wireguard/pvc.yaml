apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wg-conf-pvc
  namespace: vpn
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 50Mi