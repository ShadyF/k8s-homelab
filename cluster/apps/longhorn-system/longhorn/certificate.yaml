apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: longhorn-tls-certificate
  namespace: longhorn
spec:
  secretName: longhorn-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: longhorn-tls
  dnsNames:
    - longhorn-tls